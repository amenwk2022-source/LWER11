name: 🚀 Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4
      - name: 🔧 Setup Pages
        uses: actions/configure-pages@v4
      - name: 📦 Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'
      - name: 🌐 Deploy
        id: deployment
        uses: actions/deploy-pages@v4
```

اضغط **Commit changes** ← **Commit directly to main**

---

## الخطوة 2 — رفع ملفات التطبيق

اضغط **Code** (في القائمة العلوية) ← ثم **Add file** ← **Upload files**

ارفع هذه الملفات من الـ ZIP الذي نزّلته:
- `index.html` ✅
- `manifest.json` ✅
- `sw.js` ✅
- مجلد `icons/` (ارفع الملفات داخله) ✅

اضغط **Commit changes**

---

## الخطوة 3 — تفعيل GitHub Pages

اذهب لـ **Settings** ← **Pages** (من القائمة الجانبية)

تحت **Source** اختر: **GitHub Actions**

---

## الخطوة 4 — تأكد الـ Actions تشتغل

اضغط تبويب **Actions** ← ستشوف workflow يشتغل ← بعد دقيقتين يصبح ✅

رابط موقعك سيكون:
```
https://amenwk2022-source.github.io/LWER11/
