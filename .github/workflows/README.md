# ⚖️ مكتب النخبة للمحاماة — دليل النشر الكامل

> نظام إدارة مكتب محاماة متكامل | استضافة مجانية 100% | تطبيق جوال PWA

---

## 🗂️ محتويات هذا المجلد

```
law-firm-deploy/
├── index.html              ← التطبيق الكامل (كل الكود هنا)
├── manifest.json           ← إعدادات تطبيق الجوال PWA
├── sw.js                   ← Service Worker (وضع أوفلاين)
├── generate_icons.py       ← مولّد أيقونات التطبيق
├── supabase-schema.sql     ← قاعدة البيانات الكاملة
├── .github/
│   └── workflows/
│       └── deploy.yml      ← النشر التلقائي على GitHub Pages
└── README.md               ← هذا الملف
```

---

## 🚀 خطوات النشر (30 دقيقة فقط)

### الخطوة 1️⃣ — إنشاء قاعدة البيانات (Supabase)

1. اذهب لـ **[supabase.com](https://supabase.com)** → **Sign Up** (مجاني)
2. أنشئ مشروعاً جديداً:
   - **Name:** `nokhba-law` (أو أي اسم)
   - **Database Password:** اختر كلمة مرور قوية واحفظها
   - **Region:** اختر **Middle East (Bahrain)** لأسرع استجابة
3. انتظر حتى ينتهي الإعداد (دقيقة أو دقيقتان)

4. اذهب لـ **SQL Editor** → انسخ كامل محتوى `supabase-schema.sql` والصقه → **Run**

5. من **Settings → API** احفظ:
   - `Project URL` — يبدأ بـ `https://`
   - `anon public` key — يبدأ بـ `eyJ`

---

### الخطوة 2️⃣ — إنشاء أول مستخدم (Admin)

1. في Supabase → **Authentication → Users → Add User**
2. أدخل:
   - Email: `admin@nokhba-law.com` (أو أي إيميل)
   - Password: كلمة مرور قوية
3. بعد الإنشاء، اذهب لـ **Table Editor → staff → Insert Row**:
   ```
   user_id    : (نسخ ID المستخدم من Authentication → Users)
   name       : م. أحمد الكويتي
   title      : محامي رئيسي
   role       : admin
   phone      : +965 9999 1234
   email      : admin@nokhba-law.com
   ```

---

### الخطوة 3️⃣ — رفع الموقع على GitHub

1. اذهب لـ **[github.com](https://github.com)** → **Sign Up** (مجاني)

2. أنشئ مستودعاً جديداً:
   - **Repository name:** `nokhba-law` (أو أي اسم)
   - **Public** ✓ (مطلوب لـ GitHub Pages المجاني)
   - **Add README** ✗

3. ارفع الملفات:
   ```bash
   # الطريقة الأسهل — عبر واجهة GitHub:
   # اضغط "Upload files" وارفع كل ملفات المجلد
   
   # أو عبر Git:
   git init
   git add .
   git commit -m "🚀 Initial deployment"
   git branch -M main
   git remote add origin https://github.com/USERNAME/nokhba-law.git
   git push -u origin main
   ```

---

### الخطوة 4️⃣ — تفعيل GitHub Pages

1. في المستودع → **Settings → Pages**
2. **Source:** `GitHub Actions`
3. سيبدأ النشر تلقائياً عند كل رفع

4. رابط موقعك سيكون:
   ```
   https://USERNAME.github.io/nokhba-law/
   ```

---

### الخطوة 5️⃣ — أول تشغيل

1. افتح رابط موقعك
2. ستظهر شاشة تسجيل الدخول → اضغط **"إعداد Supabase أولاً"**
3. أدخل:
   - **Project URL** من Supabase
   - **Anon Key** من Supabase
4. اضغط **"حفظ والاتصال"**
5. سجّل الدخول بالإيميل والباسورد اللي أنشأتهم في الخطوة 2

---

## 📱 تثبيت التطبيق على الجوال

### Android
- افتح الموقع في Chrome
- ستظهر رسالة "Add to Home Screen" تلقائياً
- أو اضغط قائمة Chrome → "Install app"

### iPhone / iPad
- افتح الموقع في Safari
- اضغط زر المشاركة (Share) ↑
- اختر **"Add to Home Screen"**
- اضغط **"Add"**

### Windows / Mac
- افتح الموقع في Chrome أو Edge
- ستظهر أيقونة تثبيت في شريط العنوان

---

## 🔒 الأمان والخصوصية

- ✅ كل البيانات محفوظة في Supabase الخاص بك فقط
- ✅ تشفير SSL/TLS كامل
- ✅ Row Level Security — كل مستخدم يرى بياناته فقط
- ✅ مفاتيح Supabase محفوظة على جهازك فقط (localStorage)
- ⚠️ لا تشارك الـ `service_role` key مع أحد

---

## 🌐 دومين مخصص (اختياري)

لو تريد رابطاً مثل `nokhba-law.com`:

1. اشترِ الدومين من **Namecheap** أو **GoDaddy** (~10$/سنة)
2. في GitHub Pages Settings → **Custom domain** → أدخل دومينك
3. في إعدادات DNS أضف:
   ```
   Type: CNAME
   Name: @  (أو www)
   Value: USERNAME.github.io
   ```

---

## ♻️ التحديثات التلقائية

أي تعديل ترفعه على GitHub سينشر تلقائياً خلال 2-3 دقائق بدون أي عمل إضافي.

```bash
# لرفع تحديث:
git add .
git commit -m "تحديث: وصف التغيير"
git push
```

---

## 🆘 حل المشاكل الشائعة

| المشكلة | الحل |
|---------|------|
| "Invalid login credentials" | تحقق من الإيميل والباسورد في Supabase Auth |
| البيانات لا تُحفظ | تحقق من Project URL و Anon Key |
| الأيقونات لا تظهر | شغّل `python3 generate_icons.py` |
| الموقع لا يفتح | تأكد أن Repository اسمه يطابق رابط GitHub Pages |
| خطأ CORS | في Supabase → Settings → API → أضف رابط موقعك في Allowed Origins |

---

## 📊 حدود الخطة المجانية

| الخدمة | الحد المجاني |
|--------|-------------|
| GitHub Pages | 1GB مساحة / 100GB bandwidth شهرياً |
| Supabase Database | 500MB تخزين |
| Supabase Auth | 50,000 مستخدم شهرياً |
| Supabase Storage | 1GB للملفات |
| Supabase Bandwidth | 2GB شهرياً |

> كافٍ تماماً لمكتب محاماة صغير إلى متوسط.

---

## 📞 الدعم

لأي مشكلة في الإعداد، يمكنك:
- مراجعة [توثيق Supabase](https://supabase.com/docs)
- مراجعة [توثيق GitHub Pages](https://docs.github.com/en/pages)

---

*مكتب النخبة للمحاماة والاستشارات القانونية — الكويت* ⚖️
